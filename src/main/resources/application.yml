spring:
  data:
    mongodb:
      uri: mongodb://localhost:27017/GiftGather
      host: localhost
      port: 27017
      username:
      password:
      authentication-database: test

  datasource:
    url: jdbc:mysql://localhost:3306/GiftGather
    username: root
    password: malKong6497*
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
